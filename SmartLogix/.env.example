# Copia este archivo a ".env" (en esta misma carpeta, junto a docker-compose.yml)
# y completa tu propia API key. NUNCA subas el .env real a git ni lo compartas.
#
# La API key de Groq es GRATIS: creala en https://console.groq.com/keys

GROQ_API_KEY=
GROQ_MODEL=
